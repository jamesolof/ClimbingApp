namespace ClimbingApp.Views.Home {
    export class HomeController {
        public locations: Models.Location[];

        static $inject = [
        ]

        constructor(
        ) {
        }
    }
}